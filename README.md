# Hero Pro

这是一个基于 Next.js App Router 的基础项目骨架，包含：

- TypeScript
- ESLint
- `src/` 目录结构
- `@/*` 路径别名

## 开始使用

1. 安装 Node.js 20.9 或更高版本
2. 在项目根目录安装依赖
3. 启动开发服务器

```bash
npm install
npm run dev
```

默认访问地址：

[http://localhost:3000](http://localhost:3000)
